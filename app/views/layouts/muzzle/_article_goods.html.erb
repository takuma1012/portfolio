<!-- 詳細 -->
<%= content_tag(:div, class: ["lightbox", "cd#{g}", "clearfix"]) do %>
	<div id="jacket-show">
		<%= attachment_image_tag goods, :article_image, format: 'jpg', fallback: "/no_image_m.png", width: '350', height: '350'%>
	</div>
	<div id="cd-detail">
		<p class="title"><%= goods.article_name %></p>
		<hr width="100%">
			<% goods.tracks.each do |t| %>
			<p class="track">
				<%= t.track_num %>.<%= t.track_name %><br/>
			<% end %></p>
		<hr width="100%">
		<p class="show-price">￥<%= goods.price.to_s(:delimited) %>(tax-in)</p>
	</div>
	<%= link_to muzzle_orders_new_path(id: goods.id) do %>
	<button id="show-buy"><strong>Buy this</strong></button><% end %>
<% end %>
